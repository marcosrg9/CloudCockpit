import { OnloadTermDirective } from './onload-term.directive';

describe('OnloadTermDirective', () => {
  it('should create an instance', () => {
    const directive = new OnloadTermDirective();
    expect(directive).toBeTruthy();
  });
});
